# Generated by Cargo
# will have compiled files and executables
debug/
target/

# These are backup files generated by rustfmt
**/*.rs.bk

# MSVC Windows builds of rustc generate these, which store debugging information
*.pdb

# node_modules in static/
static/nodejs/node_modules/

# IDE
.vscode/

.envrc
.venv

tests/resources/generated/
dist

# During integration tests, the data package under test is freshly built, always
# with a distinct hash (due, at least in part, to some use of timestamps in the
# names of test entities). To prevent differences from appearing any time one
# runs integration tests, ignore the lockfile.
tests/nodejs/package-lock.json

# Python
__pycache__

# Test/Build environment not to be committed, exammple env file is .env.example
.env
